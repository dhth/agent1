# to test operations on linux
services:
  agent1-dev:
    image: rust:1.88-slim-bullseye
    platform: linux/amd64
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    command: sleep infinity
